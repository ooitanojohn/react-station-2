import styles from "@/src/styles/Threads.module.css";

export default function Threads() {
    return (
        <section className={styles.section}>
            <h3>参加するスレッドを選択してください。</h3>
        </section>
    );
}